# pnpm settings (new home as of pnpm 10+; the "pnpm" field in package.json is
# ignored by newer pnpm). Allow esbuild — pulled in by Vite — to run its install
# script, otherwise newer pnpm aborts with ERR_PNPM_IGNORED_BUILDS.
onlyBuiltDependencies:
  - esbuild
