# Disable pnpm's "minimum release age" supply-chain gate for this project.
# Some transitive dependencies are published very recently and would otherwise
# be rejected by that policy (ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION), which is
# unrelated to the safety of this project. Project .npmrc overrides global config.
minimum-release-age=0
